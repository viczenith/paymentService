def send_notification(user, message):
    """
    Function to send a notification to a user.
    This can be implemented using various methods such as email, SMS, push notifications, etc.
    """
    # Implementation of sending notification (e.g., email, SMS, push notification) to the user
    # For example, if sending an email notification:
    # Replace the following line with your actual code to send an email
    print(f"Notification sent to {user}: {message}")
